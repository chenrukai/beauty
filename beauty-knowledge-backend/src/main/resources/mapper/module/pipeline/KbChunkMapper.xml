<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beauty.knowledge.module.pipeline.mapper.PipelineKbChunkMapper">

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO kb_chunk (knowledge_id, file_id, chunk_index, page_no, content, char_count, vector_status)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.knowledgeId}, #{item.fileId}, #{item.chunkIndex}, #{item.pageNo}, #{item.content}, #{item.charCount}, #{item.vectorStatus})
        </foreach>
    </insert>
</mapper>
